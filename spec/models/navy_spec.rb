require "spec_helper"

describe Navy do
  describe "#attack" do
    context "when it is player's turn" do
      it "allows the attack"
    end

    context "when it is not player's turn" do
      it "does not allow the attack"
    end
  end
end
